import logging

# create file handler for logging
logging.basicConfig(filename='test.python.log', 
    format='%(asctime)s %(levelname)-8s %(message)s', 
    level=logging.INFO, 
    datefmt='%Y-%m-%d %H:%M:%S')